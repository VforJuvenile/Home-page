待解决的问题：

书签：
1. 添加时的验证，添加后的按钮控制，添加成功后的响应；
2. 对应视图的刷新
3. 书签自适应和自设置
4. 浏览器API接入
5. 正则表达式验证网址是否正常
6. 链接自动添加http或https?
7. 导入书签文档来读取，导出本机文档；
8. 利用路由的resolve来拉取数据后再显示，而不需要专门设置标志位来判断显示；
9. 模块化、解耦

框架：
1. 页面布局更改
2. 登陆过滤

奇怪问题1：
a.上面div浮动，下方div的子元素居然会在浮动的下方，而不是底层中； float会使后面div出现在该元素下方，但是后div的子元素不会！！！
b.子div设置height:100%可行，但是添加overflow:auto就不行了？
c.如何给float元素设置高度为100%;

已解决：
1. 做到加载不同的页面才加载他的js文件或者directive;
2. directive中的link能否在其子节点(后台加载而来)加载完成在执行;
3. 子元素absolute如何撑开父元素;已改为float;
4. ui-router代替ngRoute以及对应的复杂视图更改为单个视图;
5. 非法路径全部跳转到login;


长远问题：
1. 错误界面的跳转--直接从地址栏输入，如何辨别，每个控制器加判断？
2. 介绍页的制作；
3. 第三方登陆验证；
4. 自适应框架；
5. 保存密码，cookie;
6. 好友即时通讯;
7. 左侧隐藏，点击后弹出，其他模块上面有浮层的组件制作--通用，用户头像，设置等；
8. 书签同名处理；
9. 代码同步调试，前台；
10.富文本编辑器；